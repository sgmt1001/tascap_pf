<div class="container">
  <div class="row my-3">
    <div class="col-lg-12">
      <h1>belonging user</h1>
      <h4>organization:<%= @organization.name %></h4>
      <div><%= link_to 'add user',organization_belonging_search_path(@organization) %></div>
      <table class='table'>
        <thead>
          <tr>
            <th style="width :20%">user</th>
            <th style="width :60%">introduction</th>
            <th style="width :20%"></th>
          </tr>
        </thead>
        <tbody>
          <% @organization.users.each do |user| %>
            <tr>
              <td>
                <span><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no-image-icon.jpeg", size: '40x40') %></span>
                <span><%= user.name %></span>
              </td>
              <td><%= user.introduction %></td>
              <% if user.id != @organization.user_id %>
              <td><%= link_to 'delete',organization_belonging_invite_destroy_path(@organization.id,user_id: user.id),method: :delete, data: { confirm: '本当に消しますか？' } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>