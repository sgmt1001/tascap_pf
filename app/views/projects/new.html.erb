<div class="container">
  <div class="row my-3">
    <div class="col-lg-12">
      <h1>new project</h1>

      <%= form_with model:@project,url:organization_projects_path,local:true do |f| %>
        <div>
          <%= f.label :name %><br>
          <%= f.text_field :name, size: "60x10" %>
        </div>
        <div>
          <%= f.label :overview %><br>
          <%= f.text_area :overview, size: "60x10" %>
        </div>
        <div>
          <%= f.submit class: 'btn btn-primary' %>
        </div>
      <% end %>

    </div>
  </div>
</div>